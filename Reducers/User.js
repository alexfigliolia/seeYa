const initialState = {
	username: 'Alex Figliolia',
	email: 'alexfigliolia@gmail.com',
	userID: null,
}

export default (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
}